{
  "name": "paginate-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for the paginate engine: the Rust core plus the Python (pypaginate) and Node/TypeScript (@cyblow/paginate) adapters.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/CybLow/paginate"
  },
  "workspaces": [
    "ts",
    "crates/node"
  ],
  "scripts": {
    "build:node": "cd crates/node && bun run build",
    "test:ts": "cd ts && bun run test"
  }
}

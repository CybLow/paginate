pypaginate.filters.search.helpers
=================================

.. py:module:: pypaginate.filters.search.helpers

.. autoapi-nested-parse::

   Helper primitives shared by SQL search strategies.



Classes
-------

.. autoapisummary::

   pypaginate.filters.search.helpers.ClauseBuilder


Functions
---------

.. autoapisummary::

   pypaginate.filters.search.helpers.clause_sequences
   pypaginate.filters.search.helpers.collect_clauses
   pypaginate.filters.search.helpers.column_attributes
   pypaginate.filters.search.helpers.like_for_fields
   pypaginate.filters.search.helpers.match_columns
   pypaginate.filters.search.helpers.matching_ids
   pypaginate.filters.search.helpers.normalize_fields
   pypaginate.filters.search.helpers.phrase_clause_factory
   pypaginate.filters.search.helpers.term_clause_factory


Module Contents
---------------

.. py:class:: ClauseBuilder

   Bases: :py:obj:`Protocol`


   Base class for protocol classes.

   Protocol classes are defined as::

       class Proto(Protocol):
           def meth(self) -> int:
               ...

   Such classes are primarily used with static type checkers that recognize
   structural subtyping (static duck-typing).

   For example::

       class C:
           def meth(self) -> int:
               return 0

       def func(x: Proto) -> int:
           return x.meth()

       func(C())  # Passes static type check

   See PEP 544 for details. Protocol classes decorated with
   @typing.runtime_checkable act as simple-minded runtime protocols that check
   only the presence of given attributes, ignoring their type signatures.
   Protocol classes can be generic, they are defined as::

       class GenProto(Protocol[T]):
           def meth(self) -> T:
               ...


.. py:function:: clause_sequences(context: pypaginate.filters.search.strategies.ConditionContext, terms: collections.abc.Sequence[str], build_clause: ClauseBuilder) -> collections.abc.Iterable[pypaginate.types.SqlClause]

   Yield non-null clauses generated by build_clause for each term.

   :param context: Strategy context.
   :param terms: Tokens to process.
   :param build_clause: Clause builder function.

   :Yields: SQL clauses for each term.


.. py:function:: collect_clauses(context: pypaginate.filters.search.strategies.ConditionContext, terms: collections.abc.Sequence[str], build_clause: ClauseBuilder) -> list[pypaginate.types.SqlClause]

   Collect non-null clauses by applying a builder to all terms.

   :param context: Strategy context provided to the builder.
   :param terms: Normalized tokens for the clause factory.
   :param build_clause: Callable converting a token into a SQL clause or None.

   :returns: List of SQL clauses, skipping None results.


.. py:function:: column_attributes(model_class: type, fields: collections.abc.Sequence[str]) -> tuple[pypaginate.types.SqlStringExpression, Ellipsis]

   Return typed column expressions for the provided field names.

   :param model_class: ORM model class.
   :param fields: Field names to resolve.

   :returns: Tuple of column expressions.


.. py:function:: like_for_fields(normalizer: pypaginate.text.api.SqlTextNormalizer, model_class: type, fields: collections.abc.Sequence[str], token: str, *, prefix: bool) -> list[pypaginate.types.SqlClause]

   Return a single OR-ed LIKE clause across fields for token.

   :param normalizer: SQL text normalizer.
   :param model_class: ORM model class.
   :param fields: Field names to search.
   :param token: Search token.
   :param prefix: Whether to use prefix matching.

   :returns: List with a single OR-ed LIKE clause.


.. py:function:: match_columns(columns: collections.abc.Sequence[pypaginate.types.SqlStringExpression], tokens: set[str]) -> pypaginate.types.SqlClause | None

   Build a clause matching any of the provided tokens across columns.

   :param columns: Column expressions to search.
   :param tokens: Set of values to match.

   :returns: A SQL IN clause or None if no columns/tokens.


.. py:function:: matching_ids(context: pypaginate.filters.search.strategies.ConditionContext) -> set[str]

   Return tokens matching the configured ID pattern.

   :param context: Strategy context carrying tokens and ID pattern.

   :returns: A set of raw tokens considered identifiers.


.. py:function:: normalize_fields(normalizer: pypaginate.text.api.SqlTextNormalizer, model_class: type, fields: collections.abc.Sequence[str], token: str, *, prefix: bool) -> collections.abc.Iterable[pypaginate.types.SqlClause]

   Yield LIKE clauses for each field normalised with the given token.

   :param normalizer: SQL text normalizer.
   :param model_class: ORM model class.
   :param fields: Field names.
   :param token: Search token.
   :param prefix: Whether to use prefix matching.

   :Yields: LIKE clauses for each field.


.. py:function:: phrase_clause_factory(normalizer: pypaginate.text.api.SqlTextNormalizer) -> ClauseBuilder

   Create a clause builder for phrase matching.

   :param normalizer: SQL text normalizer.

   :returns: A ClauseBuilder function for phrases.


.. py:function:: term_clause_factory(normalizer: pypaginate.text.api.SqlTextNormalizer, ignored: set[str]) -> ClauseBuilder

   Create a clause builder for term matching with ignored tokens.

   :param normalizer: SQL text normalizer.
   :param ignored: Set of tokens to skip.

   :returns: A ClauseBuilder function for terms.


